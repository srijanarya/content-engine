# Local model routing — credentials, NEVER commit
monitor/.env.local
.env
.env.*
!.env.template

# Runtime state — not source
monitor/seen_ai.json
monitor/seen_finance.json
monitor/LEARNINGS.md
monitor/*.log
monitor/run.log
monitor/run.err

# Generated content — published on channels, not versioned here.
# (Sample drafts kept under drafts/examples/ are tracked; see below.)
drafts/*.md
!drafts/examples/

# Performance data — real engagement numbers stay local
content_performance.json

# Internal business strategy + unreleased product/sales/growth drafts — local only, never public
STRATEGY.md
product/
growth/

# Couples to the private trading system / personal watchlist — local only
monitor/daily_market_post.py
compliance/tickers.txt
compliance/regime_safe*.json

# X auto-post lane — couples to live X credentials; never public
post_x.py
posted_log.json
receipt_drip.py
ingest_performance.py
check_posts.py
# Browser transport, reply engine, cron + staged launchd, kill-switch, debug shots — all couple to live X
x/
# Reply pipeline runtime state + drafted/approved/posted replies
reply-pipeline/
# Live market data the finance posts render from (generated runtime state)
monitor/regime_safe.json

# Generated video content + internal handoff (engine code is public; output/strategy stays local)
video/out/
video/HANDOFF.md

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# X auto-post setup notes — local only
X_SETUP.md

# Red-team eval samples (generated, local)
compliance/redteam_samples.json

# Internal strategy / research / content queue / model evals — local only, never public.
# (untracked + previously un-ignored; one careless `git add -A` would push these to the PUBLIC repo)
CONTENT-FACTORY-BRIEF.md
PLAYBOOK.md
post-queue/
research/
evals/
